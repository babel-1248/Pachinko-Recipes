# News Flash

A Claude Code and Codex recipe that looks for the latest news on the web around a given search query.   The recipe remembers the last time time it was run and provides newer information since then.
